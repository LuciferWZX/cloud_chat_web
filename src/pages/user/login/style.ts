import styled from 'styled-components';
const LoginBox = styled.div``;
const EmailLoginBox = styled.div`
  position: relative;

  .avatar-place {
    position: absolute;
    background-color: #1890ff;
    border-radius: 4px;
  }
  button {
  }
`;
export { LoginBox, EmailLoginBox };
